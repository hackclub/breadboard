# Editor backend environment
# Copy to editor-backend/.env only when running the FastAPI backend directly.

# Frontend origin allowed during local development.
FRONTEND_URL=http://localhost:3000

# Keep compiled ESP-IDF build directories between runs for faster rebuilds.
VELXIO_PERSISTENT_BUILD_DIR=1
IDF_CCACHE_ENABLE=1
